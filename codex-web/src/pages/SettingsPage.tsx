import type { Theme } from "../types/codex";
export function SettingsPage({ theme, onThemeChange }: { theme: Theme; onThemeChange: (theme: Theme) => void }) { return <div className="settings-layout"><section className="panel settings-section"><span className="eyebrow">APPEARANCE</span><h2>Your workspace</h2><p>Choose a theme designed for focused, long-running development work.</p><div className="theme-options">{(["system", "light", "dark"] as Theme[]).map((option) => <button className={theme === option ? "theme-option selected" : "theme-option"} key={option} onClick={() => onThemeChange(option)}><span className={`theme-preview ${option}`}><i /><i /><i /></span><b>{option}</b><small>{option === "system" ? "Match device preference" : `${option} workspace`}</small></button>)}</div></section><section className="panel settings-section"><span className="eyebrow">AI ROUTING</span><h2>Model selection</h2><p>The backend announces available providers and models. This UI only submits your preference.</p><div className="settings-fields"><label>Provider<select defaultValue="automatic"><option value="automatic">Automatic routing</option><option>OpenAI</option><option>Anthropic</option></select></label><label>Model<select defaultValue="default"><option value="default">Backend selected</option><option>High reasoning</option><option>Fast execution</option></select></label></div><button className="primary">Save preferences</button></section><section className="panel settings-section security"><span className="eyebrow">NOTIFICATIONS & SECURITY</span><h2>Control signals</h2><Toggle label="Task and goal notifications" description="Show in-app notices for meaningful execution events." checked /><Toggle label="Browser notifications" description="Use browser notifications after you grant permission." /><Toggle label="Approval reminders" description="Highlight pending security or execution approvals." checked /></section></div> }
function Toggle({ label, description, checked = false }: { label: string; description: string; checked?: boolean }) { return <label className="toggle-row"><div><b>{label}</b><small>{description}</small></div><input type="checkbox" defaultChecked={checked} /><i /></label> }
