import { useState } from "react";
import type { TerminalEntry } from "../types/codex";
export function TerminalPage({ entries }: { entries: TerminalEntry[] }) { const [query, setQuery] = useState(""); return <div className="terminal-layout"><section className="terminal-window"><div className="terminal-bar"><div><i /><i /><i /></div><span>Codex output · read only</span><button aria-label="Clear terminal">⌫</button></div><div className="terminal-content">{entries.map((entry) => <pre className={entry.type} key={entry.id}>{entry.content}</pre>)}</div></section><aside className="panel terminal-side"><span className="eyebrow">COMMAND HISTORY</span><h2>Execution log</h2><p>Commands are executed only by the secure Codex backend—not by this browser.</p><label>Filter output<input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search log" /></label><div className="command-summary"><b>Last command</b><code>pnpm test integration</code><span className="status-chip running">running</span></div></aside></div> }
